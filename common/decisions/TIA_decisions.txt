TIA_ai_decision_category = {
	TIA_set_region39_blocked_rate_to_3 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_39_rate_ai = 3 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region39_blocked_rate_to_2 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_39_rate_ai = 2 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region39_blocked_rate_to_1 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_39_rate_ai = 1 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region43_blocked_rate_to_3 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_43_rate_ai = 3 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region43_blocked_rate_to_2 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_43_rate_ai = 2 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region43_blocked_rate_to_1 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_43_rate_ai = 1 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region13_blocked_rate_to_3 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_13_rate_ai = 3 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region13_blocked_rate_to_2 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_13_rate_ai = 2 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region13_blocked_rate_to_1 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_13_rate_ai = 1 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region26_blocked_rate_to_3 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_26_rate_ai = 3 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region26_blocked_rate_to_2 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_26_rate_ai = 2 }
		}
		ai_will_do = {
			base = 0
		}
	}
	TIA_set_region26_blocked_rate_to_1 = {
		allowed = { tag = TIA }
		visible = {
			OR = {
				is_ai = yes
				is_debug = yes
			}
		}
		available = { is_ai = yes }
		complete_effect = {
			set_variable = { TIA_block_26_rate_ai = 1 }
		}
		ai_will_do = {
			base = 0
		}
	}
}

